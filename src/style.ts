import { css } from 'lit';

export const coreCSS = css`
    .ap-carousel {
        overflow: hidden;
    }
    
    .ap-carousel-slides {
        display: flex;
        -webkit-overflow-scrolling: touch;
    }
`;